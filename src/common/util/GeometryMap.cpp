#include "GeometryMap.h"

#include <string>

namespace rocket { namespace util {
/*
void testgm() {
	std::vector<std::string> strings;

	GeometryMap<std::string> gm(Dimension(1, 1, 1));
	gm.add("Hello", glm::vec3(0,0,0));
	gm.add("world", glm::vec3(1,0,0));
	gm.add("!", glm::vec3(2,0,0));
	gm.remove("Hello");
	gm.move("world", glm::vec3(0,1,0));
	gm.resize(Dimension(2,2,1));

	gm.get(glm::vec3(0,0,0), 1, strings);
}
*/

}}
